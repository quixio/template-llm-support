name: Chatbot
language: python
variables:
  - name: topic
    inputType: OutputTopic
    description: Chat topic
    defaultValue: ''
    required: true
  - name: role
    inputType: FreeText
    description: 'Chatbot role: AGENT, CUSTOMER'
    defaultValue: ''
    required: true
dockerfile: build/dockerfile
runEntryPoint: main.py
defaultFile: main.py
